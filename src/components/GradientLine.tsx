export default function GradientLine() {
  return (
    <div
      className="line bg-gradient-to-r pb-[1px] dark:from-bg-dark dark:via-lineColor-dark dark:to-bg-dark
    from-bg via-bg-dark to-bg w-full"
    ></div>
  );
}
